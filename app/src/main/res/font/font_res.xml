<?xml version="1.0" encoding="utf-8"?>
<font-family xmlns:android="http://schemas.android.com/apk/res/android"
             xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- regular -->
    <font
        android:font="@font/roboto_regular"
        android:fontStyle="normal"
        android:fontWeight="500"

        app:font="@font/roboto_regular"
        app:fontStyle="normal"
        app:fontWeight="500"/>

    <!-- bold -->
    <font
        android:font="@font/roboto_bold"
        android:fontStyle="normal"
        android:fontWeight="700"

        app:font="@font/roboto_bold"
        app:fontStyle="normal"
        app:fontWeight="700"/>

    <!-- medium -->
    <font
        android:font="@font/roboto_medium"
        android:fontStyle="normal"
        android:fontWeight="600"

        app:font="@font/roboto_medium"
        app:fontStyle="normal"
        app:fontWeight="600"/>

    <!-- light -->
    <font
        android:font="@font/roboto_light"
        android:fontStyle="normal"
        android:fontWeight="400"

        app:font="@font/roboto_light"
        app:fontStyle="normal"
        app:fontWeight="400"/>

    <!-- sfuid semibold -->
    <font
        android:font="@font/sfuidisplay_semibold"
        android:fontStyle="normal"
        android:fontWeight="400"

        app:font="@font/sfuidisplay_semibold"
        app:fontStyle="normal"
        app:fontWeight="400"/>

    <!-- sfuid bold -->
    <font
        android:font="@font/sfuidisplay_bold"
        android:fontStyle="normal"
        android:fontWeight="400"

        app:font="@font/sfuidisplay_bold"
        app:fontStyle="normal"
        app:fontWeight="400"/>

    <!-- sfuid heavy -->
    <font
        android:font="@font/sfuidisplay_heavy"
        android:fontStyle="normal"
        android:fontWeight="400"

        app:font="@font/sfuidisplay_heavy"
        app:fontStyle="normal"
        app:fontWeight="400"/>

    <!-- sfuid regular -->
    <font
        android:font="@font/sfuidisplay_regular"
        android:fontStyle="normal"
        android:fontWeight="400"

        app:font="@font/sfuidisplay_regular"
        app:fontStyle="normal"
        app:fontWeight="400"/>

</font-family>